<% if @child.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      入力エラーが <%= pluralize(@child.errors.count, "件あります。") %>
    </div>
    <ul>
    <% @child.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
