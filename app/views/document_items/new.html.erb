<% provide(:item_url,document_item_create_path(@document)) %>
<div class="text-center text-primary">
 <ul class="document_ul">
    <li id="document_img"><%= image_tag('/explain/form1.png',size:"60x60",style:"display:inline") %><li>
    <li><h1 class="title_font">質問項目のフォーム作成</h1></li>
  </ul>
</div>
<hr>
<h2 class="title_font" style="font-size:24px;"><%= "タイトル:#{@document.title}" %><br><br>
<% if flash[:danger].present? %>
 <div class="alert alert-danger" id="item_flash"><%= flash[:danger] %></div>
<% end %>
<%= render 'form', document_item: @document_item %>

<style>
  body{ background-color:#00FF00;}
</style>
<script>
  var flg = 1;
  $(function(){
   // 自身のページを履歴に追加
   history.pushState(null, null, null);
   // ページ戻り時にも自身のページを履歴に追加
   $(window).on("popstate", function(){
       history.pushState(null, null, null);
   });
     $(window).on("beforeunload",function(e){
   if(flg) return "本当に離れますか？";
 });
  $('#form_id').submit(function(){
    flg = 0;
  });

  });
</script>